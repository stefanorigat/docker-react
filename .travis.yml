language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t stefanorigat/docker-react -f Dockerfile.dev .

script:
    - docker run stefanorigat/docker-react npm run test
    
